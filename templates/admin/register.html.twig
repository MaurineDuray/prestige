{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    {% include "/partials/header.html.twig" %}
    <div class="slide" id="admin">
        <h1>Administration - Membres</h1>
        <a href="{{path('admin_membres')}}"><button class="btn-dore">Retour aux membres </button></a>
        <div class="liste">
          {{form_start(myform)}}
            <div class="container" style="background-color:  rgba(255, 170, 22, 0.1); padding: 20px; border-radius: 20px;">
                <div>
                    
                    {{form_widget(myform)}}

                </div>
                <button type="submit" class="btn-dore"><i class="fa-solid fa-check"></i> Ajouter le membre</button>
            </div>
            
        {{form_end(myform)}}
        </div>
    </div>
    {% include "/partials/footer.html.twig" %}
{% endblock %}
